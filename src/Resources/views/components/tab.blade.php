<button class="mfw-tab nav-link{{ $active  ? ' active' : ''}}" id="{{ $tag }}-tab" data-bs-toggle="tab"
        data-bs-target="#{{ $tag }}" type="button" role="tab" aria-controls="{{ $tag }}"
        aria-selected="true">{{ $label }}</button>
